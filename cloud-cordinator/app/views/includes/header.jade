header#header
  .navbar.navbar-static-top
    .navbar-inner(role='navigation')
      .container
        a.brand(href='/') Home Monitor Cloud Cordinator 
        .nav-collapse.collapse
          ul.nav
            li
              a(href='/site/sensors') Dashboard
          ul.nav.pull-right
            if (req.isAuthenticated())
              li
                a(href="/users/"+req.user.id, title="Profile") #{req.user.name}
              li
                a(href="/logout", title="logout") Logout
            else
              li
                a(href="/login", title="Login") Login
